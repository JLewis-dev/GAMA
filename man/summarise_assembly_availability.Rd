% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Availability_Analysis.R
\name{summarise_assembly_availability}
\alias{summarise_assembly_availability}
\title{Summarise Assembly composition}
\usage{
summarise_assembly_availability(results, species = NULL)
}
\arguments{
\item{results}{A list returned by \code{\link[=query_species]{query_species()}}.}

\item{species}{\code{NULL} (default) to include all species, or a character
vector specifying which species to include.}
}
\value{
A tibble with one row per species and the following columns:
\itemize{
\item \code{species}: species name
\item \code{Assembly}: total Assembly accession count
\item \code{complete}, \code{chromosome}, \code{scaffold}, \code{contig}: assembly level counts
\item \code{best_n50}: highest N50 among assemblies at the highest available
recognised assembly level
}
The tibble has class \code{gdt_tbl} and carries a \code{query_info} attribute for
provenance.
}
\description{
Collapses Assembly metadata into species-level assembly level counts.
}
\details{
\code{summarise_assembly_availability()} reports the total number of Assembly
accessions returned by \code{\link[=query_species]{query_species()}} and the number assigned to each
recognised assembly level: \code{complete}, \code{chromosome}, \code{scaffold}, and
\code{contig}. Assembly level counts are derived from Assembly esummary metadata;
the \code{Assembly} total is retained from the original query accession count.

The \code{best_n50} column is the highest N50 among assemblies at the highest
available recognised assembly level for each species. Assembly levels are
ranked as complete > chromosome > scaffold > contig. Where multiple
assemblies are present at the highest available level, the highest N50
among those assemblies is reported. If no recognised assembly level with an
available N50 is found, \code{best_n50} is returned as \code{NA_real_}.
}
\examples{
\dontrun{
RESULTS <- query_species(c('Vigna angularis', 'Vigna vexillata'))
ASM_SUMMARY <- summarise_assembly_availability(RESULTS)
print(ASM_SUMMARY)
}
}
\seealso{
\code{\link[=query_species]{query_species()}}, \code{\link[=plot_assembly_availability]{plot_assembly_availability()}},
\code{\link[=extract_assembly_metadata]{extract_assembly_metadata()}}
}
