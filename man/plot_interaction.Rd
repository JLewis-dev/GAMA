% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plotting.R
\name{plot_interaction}
\alias{plot_interaction}
\title{Plot interaction summaries}
\usage{
plot_interaction(
  INTERACTION,
  species = NULL,
  value = "count",
  rank = "highest",
  theme_fn = ggplot2::theme_minimal,
  low_fill = "#E3F9F2",
  high_fill = "#56C5A8",
  positive_low_fill = "#FADDD4",
  positive_high_fill = "#DC7D67",
  negative_low_fill = "#EDE5F6",
  negative_high_fill = "#8D65B0",
  zero_fill = "#F2F2F2",
  na_fill = "#BDBDBD",
  show_values = TRUE,
  value_size = 2.4
)
}
\arguments{
\item{INTERACTION}{A summary table returned by \code{\link[=summarise_interaction]{summarise_interaction()}}.}

\item{species}{\code{NULL} (default) to plot all species, or a character vector
specifying which species to include.}

\item{value}{Heatmap value to display. One of \code{count} or \code{residual}.}

\item{rank}{Ordering of species. One of \code{highest} (default), \code{lowest},
\code{A-Z}, \code{Z-A}, or \code{input}.}

\item{theme_fn}{A ggplot2 theme function.}

\item{low_fill}{Low count fill colour.}

\item{high_fill}{High count fill colour.}

\item{positive_low_fill}{Low positive residual fill colour.}

\item{positive_high_fill}{High positive residual fill colour.}

\item{negative_low_fill}{Low negative residual fill colour.}

\item{negative_high_fill}{High negative residual fill colour.}

\item{zero_fill}{Zero residual fill colour.}

\item{na_fill}{Missing value fill colour.}

\item{show_values}{Logical; if \code{TRUE} (default), show cell values.}

\item{value_size}{Numeric text size for cell values.}
}
\value{
A ggplot object for a single species, or a named list of ggplot
objects when multiple species are plotted.
}
\description{
Visualises interaction summaries as modality-by-anatomy heatmaps for one
or more species.
}
\details{
\code{plot_interaction()} operates on the summary returned by
\code{\link[=summarise_interaction]{summarise_interaction()}}. The plotted anatomy resolution is inherited from
the \code{INTERACTION} object: if the summary was generated with
\code{level = 'anatomy_class'}, columns represent anatomy classes; if it was
generated with \code{level = 'anatomy_subclass'}, columns represent anatomy
subclasses.

With \code{value = 'count'}, the heatmap shows the observed number of linked
BioSample records in each SRA modality-by-anatomy cell.

With \code{value = 'residual'}, the heatmap shows Pearson residuals from the
species-level marginal expectation calculated by \code{\link[=summarise_interaction]{summarise_interaction()}}.
Positive residuals indicate modality-anatomy combinations that are
over-represented relative to the marginal distributions; negative residuals
indicate under-represented combinations.

When multiple species are plotted, species can be ordered with \code{rank}.
The ranking is applied to the filtered interaction summary before plots are
generated.
}
\examples{
\dontrun{
RESULTS <- query_species(c('Vigna angularis', 'Vigna vexillata'))
SRA_SUMMARY <- summarise_sra_availability(RESULTS)
BIO_SUMMARY <- summarise_biosample_availability(RESULTS)
INTERACTION <- summarise_interaction(
  SRA_SUMMARY,
  BIO_SUMMARY,
  species = 'Vigna angularis'
)
plot_interaction(
  INTERACTION,
  value = 'count'
)
plot_interaction(
  INTERACTION,
  value = 'residual'
)
}
}
\seealso{
\code{\link[=summarise_interaction]{summarise_interaction()}}, \code{\link[=summarise_sra_availability]{summarise_sra_availability()}},
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}
}
