% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Diagnostics.R
\name{report_diagnostics}
\alias{report_diagnostics}
\title{Report GAMA diagnostics}
\usage{
report_diagnostics(x, view = "recoverability")
}
\arguments{
\item{x}{A data frame or tibble returned by \code{\link[=summarise_sra_availability]{summarise_sra_availability()}},
\code{\link[=summarise_sra_skew]{summarise_sra_skew()}}, \code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}},
\code{\link[=summarise_biosample_skew]{summarise_biosample_skew()}}, or \code{\link[=summarise_interaction]{summarise_interaction()}}.}

\item{view}{Diagnostic view: either \code{recoverability} (default) or \code{records}.}
}
\value{
A tibble with class \code{gdt_tbl} carrying the input \code{query_info}
attribute. The recoverability view contains one row per species, the
relevant denominator, diagnostic counts, and \code{recoverability}; the
records view contains object-specific identifiers and diagnostic fields.
}
\description{
Reports metadata recoverability and identifies records with unresolved
classifications or missing linkage identifiers in supported GAMA objects.
}
\details{
\code{report_diagnostics()} accepts output from \code{\link[=summarise_sra_availability]{summarise_sra_availability()}},
\code{\link[=summarise_sra_skew]{summarise_sra_skew()}}, \code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}},
\code{\link[=summarise_biosample_skew]{summarise_biosample_skew()}}, or \code{\link[=summarise_interaction]{summarise_interaction()}}. Diagnostic data
are derived from summary fields, record-level profiles, and recovery data.

\code{recoverability} is the proportion of the relevant record denominator for
which the required classification or linkage information was recovered.
Values range from 0 to 1 and are calculated from the returned count columns:
\itemize{
\item SRA availability: \code{(SRA - unknown) / SRA}
\item SRA skew: \code{linked / SRA}
\item BioSample availability: \code{(operable - unknown) / BioSample}
\item BioSample skew: \code{linked / BioSample}
\item interaction: \code{(linked - unknown) / BioSample}
}
\code{unknown} counts SRA records whose modality metadata remain missing or
uninformative after fallback parsing and BioSample records with missing-like
sample-source values or no anatomy ontology match. Non-missing,
interpretable SRA strategies outside the modality ontology are classified as
\code{other}. In interaction diagnostics, each linked BioSample is counted once
when either or both classifications are unknown. For skew diagnostics,
\code{linked} counts records carrying the active identifier. A zero denominator
produces \code{NA_real_}.

With \code{view = 'recoverability'}, the function returns one row per species.
With \code{view = 'records'}, it returns unknown SRA modalities, operable
BioSample records with unknown anatomy, interaction BioSample records that
are unlinked or have unknown modality or anatomy, or skew records missing
the active linkage identifier. If none are found, an empty table with the
complete typed column schema is returned and a message is emitted.
}
\examples{
\dontrun{
RESULTS <- query_species(c('Vigna angularis', 'Vigna vexillata'))
BIO_SUMMARY <- summarise_biosample_availability(RESULTS)
DIAGNOSTICS <- report_diagnostics(BIO_SUMMARY)
print(DIAGNOSTICS)
}

}
\seealso{
\code{\link[=summarise_sra_availability]{summarise_sra_availability()}}, \code{\link[=summarise_sra_skew]{summarise_sra_skew()}},
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}, \code{\link[=summarise_biosample_skew]{summarise_biosample_skew()}},
\code{\link[=summarise_interaction]{summarise_interaction()}}
}
