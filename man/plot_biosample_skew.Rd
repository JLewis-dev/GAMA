% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plotting.R
\name{plot_biosample_skew}
\alias{plot_biosample_skew}
\title{Plot BioSample replication skew}
\usage{
plot_biosample_skew(
  BIO_SKEW,
  species = NULL,
  rank = "highest",
  abbreviate = TRUE,
  theme_fn = ggplot2::theme_minimal,
  colours = c(box = "#E3F9F2", line = "black"),
  show_points = TRUE,
  point_colour = "#56C5A8",
  point_alpha = 0.25,
  show_labels = TRUE,
  label_digits = 1L
)
}
\arguments{
\item{BIO_SKEW}{A tibble returned by \code{\link[=summarise_biosample_skew]{summarise_biosample_skew()}}.}

\item{species}{\code{NULL} (default) to include all species, or a character
vector of species to plot.}

\item{rank}{Ordering of species. One of \code{highest} (default), \code{lowest},
\code{A-Z}, \code{Z-A}, or \code{input}.}

\item{abbreviate}{Logical; if \code{TRUE} (default), abbreviate species labels.}

\item{theme_fn}{A ggplot2 theme function.}

\item{colours}{Named character vector for box fill and line colours.}

\item{show_points}{Logical; if \code{TRUE} (default), overlay per-BioProject
points when cached profiles are available.}

\item{point_colour}{Character scalar giving the colour of overlaid points.}

\item{point_alpha}{Numeric alpha value for overlaid points.}

\item{show_labels}{Logical; if \code{TRUE} (default), label each box with
\code{eff} and \code{n}.}

\item{label_digits}{Integer; decimal places for \code{eff} labels.}
}
\value{
A ggplot object.
}
\description{
Visualises BioSample replication skew using the summary output of
\code{\link[=summarise_biosample_skew]{summarise_biosample_skew()}}. Boxplots are drawn from pre-computed summary
statistics on a log10 y-axis.
}
\details{
Optional labels show \verb{eff=<x> (n=<y>)} for each species. When a cached
BioSample-level profile is available as
\code{attr(BIO_SKEW, 'biosample_anatomy_profile')}, per-BioProject points can be
overlaid with horizontal jitter.
}
\examples{
\dontrun{
RESULTS <- query_species(c('Vigna angularis', 'Vigna vexillata'))
BIO_SUMMARY <- summarise_biosample_availability(RESULTS)
BIO_SKEW <- summarise_biosample_skew(BIO_SUMMARY, anatomy_class = 'aerial')
plot_biosample_skew(BIO_SKEW, rank = 'input')
}

}
\seealso{
\code{\link[=summarise_biosample_skew]{summarise_biosample_skew()}}
}
