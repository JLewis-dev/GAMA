% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Availability_Analysis.R
\name{summarise_sra_skew}
\alias{summarise_sra_skew}
\title{Summarise SRA replication skew across BioProjects / BioSamples}
\usage{
summarise_sra_skew(
  x,
  species = NULL,
  unit = c("bioproject", "biosample"),
  class = NULL
)
}
\arguments{
\item{x}{A data.frame/tibble returned by summarise_sra_availability()
that has a cached profile attached as attribute 'sra_profile'.}

\item{species}{Optional character vector of species names to filter the
output. If NULL, all species in x are returned.}

\item{unit}{Character scalar; either 'bioproject' (default) or 'biosample'.}

\item{class}{Optional character scalar specifying a single modality class.}
}
\value{
A tibble/data.frame with one row per species containing:
\code{species}, \code{BioProject}/\code{BioSample} (number of distinct units with records),
\code{class}, \code{min}, \code{q25}, \code{med}, \code{q75}, \code{max} (experiments per unit), and
\code{eff} (effective number of units; inverse Simpson index).
}
\description{
Quantifies replication skew across independent units (BioProject or
BioSample) using the cached UID-level SRA profile produced by
summarise_sra_availability(). Optionally computes skew within a
single modality class.
}
\details{
Profile cache (consumed by this function):
The input must carry a cached UID-level profile as attribute 'sra_profile'
containing (at minimum) 'species', 'entrez_uid', 'biosample', 'bioproject',
and 'class'. Each row in the profile corresponds to an Entrez UID.

The \code{eff} column is the \emph{effective number of units} (Hill number of order 2),
computed as the inverse Simpson index: \code{eff = 1 / sum(p^2)}, where \code{p} is the
proportion of experiments in each BioProject/BioSample. Larger values indicate
a more even spread; values near 1 indicate strong concentration in few units.
}
\examples{
\dontrun{
SRA_SUMMARY <- summarise_sra_availability(RESULTS)
SKEW <- summarise_sra_skew(SRA_SUMMARY)
print(SKEW)
}

}
