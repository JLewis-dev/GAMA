% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plotting.R
\name{plot_biosample_availability}
\alias{plot_biosample_availability}
\title{Plot BioSample anatomy composition}
\usage{
plot_biosample_availability(
  BIO,
  species = NULL,
  rank = "highest",
  abbreviate = TRUE,
  theme_fn = ggplot2::theme_minimal,
  colours = c(aerial = "#E3F9F2", ground = "#A8E6CF", reproductive = "#63C7A8", whole =
    "#329C87", in_vitro = "#1F7A73", other = "#145A52", mixed = "#062A22", unknown =
    "#BDBDBD")
)
}
\arguments{
\item{BIO}{A wide-format BioSample summary table returned by
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}.}

\item{species}{\code{NULL} (default) to plot all species, or a character vector
of species to include.}

\item{rank}{Ordering of species. One of \code{highest} (default), \code{lowest},
\code{A-Z}, \code{Z-A}, or \code{input}.}

\item{abbreviate}{Logical; if \code{TRUE} (default), abbreviate species names.}

\item{theme_fn}{A ggplot2 theme function.}

\item{colours}{Named character vector of class colours.}
}
\value{
A ggplot object showing proportional BioSample anatomy profiles
across species.
}
\description{
Visualises species-level BioSample anatomy composition using stacked
horizontal bars. Each bar shows the proportional contribution of major
anatomy classes (\code{aerial}, \code{ground}, \code{reproductive}, \code{whole}, \code{in_vitro},
\code{other}, \code{mixed}, \code{unknown}), with operable BioSample counts labelled.
}
\details{
Operates on the wide-format summary returned by
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}. Proportions are calculated over
operable BioSamples, where operable means that an accepted sample-source
attribute was present.
}
\examples{
\dontrun{
RESULTS <- query_species(c('Vigna angularis', 'Vigna vexillata'))
BIO_SUMMARY <- summarise_biosample_availability(RESULTS)
plot_biosample_availability(BIO_SUMMARY)
}
}
\seealso{
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}
}
