% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Availability_Analysis.R
\name{summarise_interaction}
\alias{summarise_interaction}
\title{Summarise SRA-BioSample interaction}
\usage{
summarise_interaction(SRA, BIO, level = "anatomy_class", species = NULL)
}
\arguments{
\item{SRA}{A summary table returned by \code{\link[=summarise_sra_availability]{summarise_sra_availability()}}.}

\item{BIO}{A summary table returned by \code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}.}

\item{level}{Anatomy resolution to summarise. One of \code{anatomy_class} or
\code{anatomy_subclass}.}

\item{species}{\code{NULL} (default) to include all species, or a character
vector specifying which species to include.}
}
\value{
A tibble with one row per species, modality class, and anatomy
category. Columns include \code{species}, \code{class}, \code{BioSample}, \code{expected}, and
\code{residual}, plus either \code{anatomy_class} or \code{anatomy_subclass}, depending on
\code{level}. The tibble has class \code{gdt_tbl}, carries a \code{query_info} attribute,
and includes interaction metadata in \code{interaction_info}.
}
\description{
Links cached SRA modality and BioSample anatomy profiles to summarise
modality-by-anatomy structure for each species.
}
\details{
Profile caches:
The input \code{SRA} must carry the cached UID-level profile produced by
\code{\link[=summarise_sra_availability]{summarise_sra_availability()}}. The input \code{BIO} must carry the cached
BioSample-level anatomy profiles produced by
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}. BioSample records are linked by shared
BioSample identifiers, then counted across SRA modality classes and either
BioSample anatomy classes or anatomy subclasses.

For each species, the \code{BioSample} column gives the observed number of linked
BioSample records in each modality-by-anatomy cell. Expected counts are
computed from the species-level marginal totals as
\code{expected = row_total * col_total / total}, where \code{row_total} is the total
number of linked records in a modality class, \code{col_total} is the total
number of linked records in an anatomy category, and \code{total} is the total
number of linked records for that species.

The \code{residual} column is computed as
\code{(BioSample - expected) / sqrt(expected)}. Positive residuals indicate
modality-anatomy combinations that are over-represented relative to the
marginal distributions; negative residuals indicate under-represented
combinations.

The meaning of \code{mixed} is tied to the requested anatomy resolution: with
\code{level = 'anatomy_class'}, \code{mixed} means more than one anatomy class was
recovered for a BioSample; with \code{level = 'anatomy_subclass'}, \code{mixed} means
more than one anatomy subclass was recovered.
}
\examples{
\dontrun{
RESULTS <- query_species(c('Vigna angularis', 'Vigna vexillata'))
SRA_SUMMARY <- summarise_sra_availability(RESULTS)
BIO_SUMMARY <- summarise_biosample_availability(RESULTS)
INTERACTION <- summarise_interaction(
  SRA_SUMMARY,
  BIO_SUMMARY,
  species = 'Vigna angularis'
)
print(INTERACTION)
}
}
\seealso{
\code{\link[=summarise_sra_availability]{summarise_sra_availability()}},
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}, \code{\link[=plot_interaction]{plot_interaction()}}
}
