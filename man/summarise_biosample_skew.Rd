% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Availability_Analysis.R
\name{summarise_biosample_skew}
\alias{summarise_biosample_skew}
\title{Summarise BioSample replication skew across BioProjects}
\usage{
summarise_biosample_skew(x, species = NULL, anatomy_class = NULL)
}
\arguments{
\item{x}{A data.frame/tibble returned by
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}} that has a cached profile attached as
attribute \code{biosample_anatomy_profile}.}

\item{species}{Optional character vector of species names to filter the
output. If \code{NULL}, all species in x are returned.}

\item{anatomy_class}{Optional character scalar specifying a single anatomy
class.}
}
\value{
A tibble/data.frame with one row per species containing:
\code{species}, \code{BioProject} (number of distinct BioProjects with operable
BioSample records), \code{anatomy_class}, \code{min}, \code{q25}, \code{med}, \code{q75}, \code{max}
(operable BioSample records per BioProject), and \code{eff} (effective number of
BioProjects; inverse Simpson index).
}
\description{
Quantifies replication skew across BioProjects using the cached
BioSample-level anatomy profile produced by
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}. Optional filters restrict the analysis
to a single anatomy class.
}
\details{
Profile cache (consumed by this function):
The input must carry a cached BioSample-level profile as attribute
\code{biosample_anatomy_profile} containing (at minimum) \code{species},
\code{biosample_id}, \code{bioproject}, and \code{anatomy_class}. Each row in the profile
corresponds to one operable BioSample record.

The \code{eff} column is the \emph{effective number of BioProjects} (Hill number of
order 2), computed as the inverse Simpson index: \code{eff = 1 / sum(p^2)},
where \code{p} is the proportion of operable BioSample records in each
BioProject. Larger values indicate a more even spread; values near 1
indicate strong concentration in few BioProjects.

Records missing BioProject IDs are excluded from the skew calculation. A
\code{skew_id_recovery} attribute reports the active denominator, number of
records included, number excluded, and recovery proportion.
}
\examples{
\dontrun{
RESULTS <- query_species(c('Vigna angularis', 'Vigna vexillata'))
BIO_SUMMARY <- summarise_biosample_availability(RESULTS)
BIO_SKEW <- summarise_biosample_skew(BIO_SUMMARY, anatomy_class = 'aerial')
print(BIO_SKEW)
}

}
\seealso{
\code{\link[=summarise_biosample_availability]{summarise_biosample_availability()}}, \code{\link[=plot_biosample_skew]{plot_biosample_skew()}}
}
