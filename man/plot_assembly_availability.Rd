% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plotting.R
\name{plot_assembly_availability}
\alias{plot_assembly_availability}
\title{Plot Assembly composition}
\usage{
plot_assembly_availability(
  ASSEMBLY,
  species = NULL,
  rank = "highest",
  abbreviate = TRUE,
  theme_fn = ggplot2::theme_minimal,
  colours = c(complete = "#E3F9F2", chromosome = "#A8E6CF", scaffold = "#56C5A8", contig
    = "#2FA083")
)
}
\arguments{
\item{ASSEMBLY}{A wide-format Assembly summary table returned by
\code{\link[=summarise_assembly_availability]{summarise_assembly_availability()}}.}

\item{species}{\code{NULL} (default) to plot all species, or a character vector
of species to include.}

\item{rank}{Ordering of species. One of \code{highest} (default), \code{lowest},
\code{A-Z}, \code{Z-A}, or \code{input}.}

\item{abbreviate}{Logical; if \code{TRUE} (default), abbreviate species names.}

\item{theme_fn}{A ggplot2 theme function.}

\item{colours}{Named character vector of assembly level colours.}
}
\value{
A ggplot object showing proportional assembly level profiles across
species.
}
\description{
Visualises species-level Assembly composition using stacked horizontal bars.
Each bar shows the proportional contribution of recognised assembly levels
(\code{complete}, \code{chromosome}, \code{scaffold}, \code{contig}), with total Assembly
accession counts labelled.
}
\details{
Operates on the wide-format summary returned by
\code{\link[=summarise_assembly_availability]{summarise_assembly_availability()}}.
}
\examples{
\dontrun{
RESULTS <- query_species(c('Vigna angularis', 'Vigna vexillata'))
ASM_SUMMARY <- summarise_assembly_availability(RESULTS)
plot_assembly_availability(ASM_SUMMARY)
}
}
\seealso{
\code{\link[=summarise_assembly_availability]{summarise_assembly_availability()}}
}
